<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Keystore keys are device-bound. Restore the vault via its recovery
         code instead of transferring unusable encrypted preferences. -->
    <exclude domain="sharedpref" path="FlutterSecureStorage.xml" />
    <exclude domain="sharedpref" path="FlutterSecureKeyStorage.xml" />
</full-backup-content>
